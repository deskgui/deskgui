/**
 * Generated by deskgui_target_icon(). Do not edit.
 *
 * Embeds the application icon and registers it with deskgui so every window
 * picks it up automatically at creation time.
 */

#include <deskgui/app_icon.h>

namespace {
  const unsigned char kDeskguiAppIcon[] = {@DESKGUI_ICON_BYTES@};

  // Registered before main() runs via this static initializer.
  const bool kDeskguiAppIconRegistered
      = ::deskgui::registerAppIcon(kDeskguiAppIcon, sizeof(kDeskguiAppIcon));
}  // namespace
